require 'spec_helper'

describe Sorteio do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Sorteio.create!(@valid_attributes)
  end
end
